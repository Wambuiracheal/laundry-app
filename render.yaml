services:
  - type: web
    name: laundry-app
    env: node
    rootDir: .
    buildCommand: npm run install:all && npm run build
    startCommand: npm run dev
    envVars:
      - key: NODE_ENV
        value: production
